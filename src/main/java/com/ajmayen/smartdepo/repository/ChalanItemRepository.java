package com.ajmayen.smartdepo.repository;

import com.ajmayen.smartdepo.model.ChalanItem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChalanItemRepository extends JpaRepository<ChalanItem, Long> {
}
